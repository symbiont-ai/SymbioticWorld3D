# Progress log

One line per passed phase: date, item, verdict, key numbers, files.
Written by the /phase coordinator after a tester PASS. Hand-entries from
the prep session are marked (manual).

- 2026-09-05 Scaffold in repo, UE 5.7, VS 2026 toolchain — PASS (manual) — Build.bat Result: Succeeded, 124 s first build, ~10 s incremental — files: Source/**, Config/**, .gitignore
- 2026-09-05 SWPlayerController + SWHUD — PASS (manual) — compiles; HUD draws global stats, meta-parameter strip, inspector, help — files: SWPlayerController.*, SWHUD.*
- 2026-09-05 Empty /Game/Maps/Valley + default map — PASS (manual) — created via -run=pythonscript Tools/make_valley_map.py — files: Content/Maps/Valley.umap, Config/DefaultEngine.ini
- 2026-09-05 Headless loop (-SWDuration self-quit, all-bin Q logging) — PASS (manual) — B seed42 300 s: 4 CSVs, exit 0, 60 s wall — files: SWWorldManager.*, SWLogger.cpp, Tools/run_sim.py
- 2026-09-05 Analysis pipeline — PASS (manual) — A: Lumen median Q drift 0.000, greedy changed 0%; B: drift 0.812, 85%; C×3 inheritance corr 0.80–0.94, |Δ| 0.022 — files: Analysis/analyze_run.py
- 2026-09-05 -SWSet reflection overrides + -SWShot self-screenshots — PASS (manual, build only) — Result: Succeeded — files: SWWorldManager.*, Tools/run_sim.py
- 2026-09-05 Windowed self-screenshot — PASS (manual) — t=4 PNG shows HUD panels, 40 Lumen, 12 Tecton, 32 patches, lit floor; -SWShot uses ':' (FParse stops at ',') — files: SWWorldManager.cpp, Tools/run_sim.py
- 2026-09-05 Founder age stagger / mode N fix — PASS (manual) — N seeds 1,2 600 s: births 175/173, Lumen held at 40, inheritance corr 0.91–0.93 — files: SWAgent.h, SWTypes.h, SWWorldManager.cpp
- 2026-09-05 Balance sweep + new default PatchRegenPerSec=6 — PASS (manual, 1 seed) — C seed1 600 s: Lumen 40→63 (min 40), Tecton 12→25, 12 generations, 348 births — files: SWTypes.h, Tools/sweep.py, DESIGN.md §6
- 2026-09-05 Manager Loop agents + /phase skill + CLAUDE.md — written, not yet exercised — files: .claude/agents/*.md, .claude/skills/phase/SKILL.md, CLAUDE.md
- 2026-09-05 Auto-select + inspector self-screenshot — PASS (manual) — seed 1, t=40: same L-0023, inherited α/ε/social unchanged, forage Q MID 0.02→+0.33, HIGH 0.00→−0.39; -SWAutoSelect ignores miss-clicks — files: SWWorldManager.*, SWPlayerController.cpp, Tools/run_sim.py
- 2026-09-05 Exact determinism — PASS (manual) — seed 7 C 300 s twice: population/births/deaths/agents.csv byte-identical (quit now checked per substep) — files: SWWorldManager.cpp
- 2026-09-05 HUD greedy row uses real feasibility mask, infeasible rows marked x — PASS (build + screenshot) — files: SWAgent.*, SWHUD.cpp
- 2026-09-05 Visual pass 1-3 (engine-only, text-authored) — PASS (build + self-screenshots) — procedural valley/river/arches/rocks (SWProcMesh, SWEnvironment), 5 generated materials (Tools/make_materials.py), procedural Lumen/Tecton bodies + glow clusters, FSWLookSettings reachable via -SWSet Look.*; environment builds in 55 ms; screenshot Saved/Screenshots/WindowsEditor/SW_20260905-103019_*_t0020.png — files: SWProcMesh.*, SWEnvironment.*, SWTypes.h, SWAgent.*, SWResourcePatch.*, SWWorldManager.*, SWGameMode.cpp, SWCameraPawn.cpp, Config/DefaultEngine.ini, Tools/make_materials.py
- 2026-09-05 Zero-click asset route — PASS — Tools/fetch_polyhaven.py: 29 CC0 Poly Haven assets (7 cliffs, 10 boulders/moss sets, 6 groundcover, 6 tileable textures), 505 MB, 112 s, needs a User-Agent header; docs/ASSET_DOWNLOADS.md written from the research inventory (Fab is click-only; Content Examples 5.7 already in the launcher vault) — files: Tools/fetch_polyhaven.py, Tools/import_assets.py, docs/ASSET_DOWNLOADS.md, .gitignore (AssetSources/)
- 2026-09-05 Imported-asset dressing — PASS (self-screenshots) — Poly Haven bundle imported in-editor (23 meshes, 94 uassets; commandlet crashes on Content Browser sync, MSYS path conversion must be off); ASWEnvironment::BuildImported scatters 48 cliffs / 110 boulders / 5000 groundcover via HISM with seeded placement; Tools/fix_foliage_materials.py wires alpha/diffuse/normal into masked two-sided materials; -SWCam=x:y:z:pitch:yaw for scripted camera shots — files: SWEnvironment.*, SWTypes.h, SWCameraPawn.cpp, Tools/import_assets.py, Tools/fix_foliage_materials.py, Tools/run_sim.py, Content/Assets/PolyHaven/**
- 2026-09-05 Tecton seams + spire — PASS (self-screenshot) — M_SW_Creature gains per-pixel crack network in local space (CrackMode/CrackScale/CrackWidth; Noise Position input connects with an empty input name), vertex colour R is now the armour-region mask; 'mountainside' slab dropped from cliff set, cliffs sunk deeper; Noise position links now work so water ripples animate — files: Tools/make_materials.py, SWProcMesh.cpp, SWAgent.cpp, SWEnvironment.cpp
- 2026-09-05 Role-based dressing + anti-aqueduct arches + Lumen refinement — PASS (self-screenshots) — FSWAssetRoles from AssetSources/ed_manifest.json (fallback: built-in Poly Haven roles), roles cliff/arch_rock/boulder/river_stone/groundcover/shrub/tree + ground/water material + Niagara mist; hero arch over the river gap + 2 companions; terrain UVs tile in world space; Lumen GI off / SSR / fog grid 16 / pool 2000 per VISUAL_PLAN §5; Lumen dotted spine + longer legs — files: SWTypes.h, SWEnvironment.*, SWProcMesh.cpp, SWWorldManager.cpp, Config/DefaultEngine.ini, Build.cs, CHECKLIST.md
- 2026-09-05 B1 lighting rewrite + look tournament — PASS (9 variants x 3 judges) — winner v4_sun_side (61/90) grafted with thin fog + warm 4000 K sun + directional inscatter; defaults baked into FSWLookSettings; frame Saved/Screenshots/WindowsEditor/SW_20260905-122730_*_t0010.png; note: Look.ConsoleCommands=stat unit crashed the DX shader worker (dxilconv.dll) once, avoid in unattended runs — files: SWTypes.h, SWEnvironment.*, SWAgent.cpp
- 2026-09-05 Electric Dreams migration + Trace X/Y — PASS (self-screenshots) — 274 packages / 3.4 GB migrated via AssetTools.MigratePackages from the vault sample (Tools/migrate_ed.py, editor on /Engine/Maps/Entry); manifest roles resolve 6/6 cliffs, 6/6 boulders, 7/7 groundcover, 4 arch rocks, 4 river stones, 3 shrubs, 2 trees, 27 mud patches, 14 mist cards, 3 drip systems; M_SW_TerrainED built from Megascans MossyGrass/MossyRockyGround/SwampWater/DriedGrass sets; Trace X (20 s half-life) and Trace Y (120 s) fields with modify/follow semantics, diminishing wI reward (0.10), ground overlay, CSV columns trace_x/trace_y/trace_X_mean/trace_Y_mean; frames SW_20260905-130812 (wide) and SW_20260905-130857 (close) — files: SWTraceField.*, SWTypes.h, SWWorldManager.*, SWAgent.*, SWLogger.*, SWEnvironment.*, SWHUD.cpp, Tools/make_materials.py, Tools/migrate_ed.py, AssetSources/ed_manifest.json, Content/{Megascans,MSPresets,Effects,FX,SmartAssets,Custom,Textures,PCG,PhysicalMaterials}
- 2026-09-05 HUD rebuilt in plate layout — PASS (self-screenshot SW_20260905-131348_*_t0038) — title block, 5 stat cards (generation, pop A/B, resources, stability = 1-(max-min)/max of total population over 30 s), species panels with behaviour-distribution bars, meta-parameter strip, ecosystem-flow minimap (water/Trace X/Trace Y/organisms/nodes from live fields), inspector with local trace values, keys panel — files: SWHUD.*, SWWorldManager.*
- 2026-09-05 e gene (EnvEffect) — PASS (headless seed 3, 400 s) — fourth inherited parameter in [0.05,1], default 0.5, σ 0.03; scales modify deposit and cost (e/0.5); logged in agents/births/deaths/population CSVs; HUD inspector + strip; analysis handles it; inheritance corr 0.94 |Δ| 0.024; Lumen mean e drifted 0.50→0.41 over 9 generations (research item) — files: SWTypes.*, SWAgent.cpp, SWWorldManager.*, SWLogger.*, SWHUD.cpp, Analysis/analyze_run.py, DESIGN.md
- 2026-09-05 e-gene adversarial verification — PASS (3 agents) — code consistency 6/6 refutations failed; mode B sd 0; C/N mutate at σ; e=0.9 run: trace means up, modify share down 15%; seed 11 twice byte-identical; TraceXDeposit lowered 0.6→0.4 so e never saturates the cell; reproducibility note added (4 Gaussians per birth now) — files: SWTypes.h, DESIGN.md
- 2026-09-05 Live run by user — mode C seed 1, 265 s at 1x-ish: Lumen 40→66, Tecton 12→55, births 205, deaths 136, max gen 6, no drought toggled
- 2026-09-05 Look diagnosis + relight (items 1-3 of the plate gap list) — PARTIAL — root cause of the "yellow wash": warm fog inscatter was the only light (sun at pitch -14 sits below the ~25° valley ridge; cooling the fog alone turns the frame black, frame SW_20260905-173047). Rig rewritten in BuildLighting: shadowless fill directional light (Look.bFillLight/FillIntensity/FillTemperature/FillPitch/FillYaw/FillColor), Look.SunSourceAngle/SunShadowDistance, ForwardShadingPriority sun 1 / fill 0 (silences the "competing directional lights" warning), cooler thinner fog, 28 defaults changed, glows halved (ResourceGlow 12, CreatureGlow 3, TrailGlow 8, WaterfallGlow 1.5, MoonGlow 2.5, LumenGlow cyan-white). Terrain checkerboard: M_SW_TerrainED never compiled since the ED migration (Megascans _N maps are virtual textures → sampler must be SAMPLERTYPE_VIRTUAL_NORMAL; UE drew WorldGridMaterial on the whole terrain); make_materials.py sample() now picks the VT sampler; terrain_ed also gained GRADIENT_TEX breakup + second tile scale + macro tone; log shows 0 "Failed to compile Material". Two 6-candidate tournaments (4 judges each, docs/plates/plate1.jpg as reference) both scored ≤5.3/10: fill ≥1.5 lux or sky light ≥3 flattens the sun; RayleighScale 0.3 = brick-red sky; RayleighScale 0.06 + clouds 0.22 = plate's blue-grey sky (frame B1, SW_20260905-181359). Interim defaults baked with the cliff-wall work; final light tune deferred until the walls exist — files: SWTypes.h, SWEnvironment.*, Tools/make_materials.py, Content/Materials/M_SW_{TerrainED,Terrain,Rock}.uasset, docs/plates/plate1..4.png
- 2026-09-05 Plate arches (user priority "I want the arches") — PASS (offscreen self-screenshots SW_20260905-191938 default cam, -192021 arch cam) — cliff-wall pass rejected: the world is 280 m across, ED cliff scans are already 15-42 m at native scale, walls at 2-2.7x dwarfed the valley (bCliffWalls now default false, CliffCount 10 at native 0.6-1.1 scale, pinnacles uniform 1.0-1.5). New SWProc::BuildMassifArch (lofted flat-topped sandstone bridge: inner/outer curves on one u, 22-vertex closed sections, shoulders 0.28, leg flare 1.9x, FBm erosion + 150 uu strata ledges, vertex R = moss mask); hero over the river (26 m opening) + twin arch with shared pier on the right slope + far small arch; cliff pieces at the feet, 40 groundcover + 3 shrubs per crown. M_SW_Sandstone (triplanar MossyRockyGround, strata band 150 uu, SandTint, moss on top) and M_SW_Scan (Albedo/Normal/DR params, DR sampler must be VIRTUAL_MASKS) — every ED rock/boulder/stone was rendering black because the ED master needs sample-only inputs; ASWEnvironment::BindScanMaterials now copies each MIC's three textures into an M_SW_Scan MID (20 meshes bound, 0 material compile failures). Lighting: golden-hour defaults baked (sun 4300 K 14 lux at pitch -6 / yaw 176, Mie 0.02, warm directional inscatter, sky light 2.5, fill 0.9, exposure +0.15) after an accidental drought frame proved the closest match to the plate; far-end terrain rise lowered on +X (0.25) so the sun glow sits behind the hero arch. Tools/run_sim.py --offscreen (-RenderOffScreen) so scripted renders no longer steal the keyboard (stray key presses had toggled mode/drought/speed mid-render). Open: sun disc itself not visible (only the glow band), strata read as regular courses, cyan trace pools dominate the floor, water bright — files: SWProcMesh.*, SWEnvironment.*, SWTypes.h, SWCameraPawn.cpp, Tools/make_materials.py, Tools/run_sim.py, Content/Materials/M_SW_{Scan,Sandstone}.uasset
- 2026-09-05 Determinism re-verified after the visual passes — PASS — seed 7 mode C 120 s headless twice: agents/births/deaths/population CSVs identical once the timestamped run_id column is stripped (8265 agent rows); environment geometry uses its own LookSeed streams, the manager stream is only consumed by agents (grep confirms)
- 2026-09-05 Finish pass: sun path, strata, arch waterfalls + mist, trace/water, glass HUD — PASS (offscreen frames SW_20260905-200136 default, -200220 arch; log clean: 0 material compile failures, 0 usage warnings, 0 LoadErrors, 0 AttachTo) — user-reported "MID_M_SW_Scan_0 missing bUsedWithInstancedStaticMeshes=True! Default Material will be used in game": every scan-bound HISM rock had silently fallen back to the default material; make_materials.py now sets used_with_instanced_static_meshes on M_SW_Scan/M_SW_Rock/M_SW_Glow (regenerated). User-reported LoadErrors for the two /Game/Meshes/_GENERATED stubs: stubs copied from the vault (gitignored under Content/Meshes/) and FindMeshes skips /_GENERATED/ so the name collision cannot pick them. Sun: the disc was behind 92% exponential fog at the horizon -> Look.FogMaxOpacity (0.7), FogDirectionalColor 0.6/0.4/0.25, Rayleigh 0.035 (golden, not red), Mie 0.008, SunSourceAngle 1.5, pitch -5; result is a golden sun path on the river from under the hero arch (was red speckles at water roughness 0.05 -> 0.12); the disc itself is still not distinct (likely under the cloud layer; try Look.bClouds=false / CloudCoverage 0.1 tomorrow). Strata: mesh ledges with per-vertex period + broken courses; material band frequency modulated by GRADIENT noise (beds widen/narrow), RustTint drift per band. Arch falls: crossed sheets from a crown point down the outer leg with drips + 2 mist cards per foot (Look.bArchFalls/ArchFallsPerArch/ArchFallWidth 180/ArchFallGlow 0.4), rim falls also crossed, drought lerps them. Trace overlay: Look.TraceOverlayIntensity 0.55 with cyan-white/amber colours; water: project M_SW_Water (dark, Fresnel-weighted, WaterBrightness 0.6). HUD: glass panels (0.38 body, 0.55 header strip, 1 px border, 1 px text shadow), same layout. Moon moved to 20/12 deg, radius 1200, glow 1.0. Glows for demo readability: CreatureGlow 5, TrailGlow 14 — files: SWTypes.h, SWEnvironment.*, SWProcMesh.cpp, SWHUD.*, Tools/make_materials.py, Tools/run_sim.py, .gitignore, Content/Materials/M_SW_{Scan,Rock,Glow,Water,Sandstone,TraceOverlay}.uasset, Content/Meshes/_GENERATED/RockFormation/*
- 2026-09-06 Hack day: Pixel Streaming for Mac collaborators — PASS (browser on 10.228.152.187 received video and input) — PixelStreaming2 plugin enabled, Epic signalling server fetched into the engine plugin folder (Tools/start_stream_server.bat), run_sim.py --stream/--offscreen; viewers open http://<host>/ ; firewall rules for the plugin's node.exe and UnrealEditor.exe already present. Incident: an implementer relinked the module DLL under the live stream (exit 0xC0000409) — Tools/build.bat now refuses to build while any UnrealEditor process exists and CLAUDE.md/agents point at it — files: SymbioticWorld.uproject, Tools/run_sim.py, Tools/start_stream_server.bat, Tools/build.bat, CLAUDE.md, .claude/agents/*, README.md
- 2026-09-06 External policy bridge (bring your own agent) — PASS (tester: regression 0 differing lines vs 2026-09-05 reference on all four CSVs, loopback 382 requests / 0 timeouts / 0.52 ms mean RTT, 2656 external decisions) — -SWPolicy="host:port=Lumen|host:port=Tecton|host:port=Both", -SWPolicyTimeoutMs, -SWPolicyShare (run_sim --policy/--policy-timeout/--policy-share); SWPolicyClient (TCP, newline JSON, hello/decide/actions/log, reconnect 5 s, fallback to the built-in bandit counted per species); agents.csv +policy, population.csv +ext_decisions,ext_fallbacks; HUD "ext N/M" + inspector policy line; Tools/policy_server.py (RandomAgent, BanditAgent, MyAgent stub; stdlib only, macOS ok), Tools/policy_client_check.py, docs/POLICY_API.md — files: SWPolicyClient.*, SWAgent.*, SWWorldManager.*, SWLogger.*, SWHUD.cpp, SWTypes.h, Build.cs, Tools/*, docs/POLICY_API.md, README.md
- 2026-09-06 Stream crashes diagnosed — exit 0xC0000409 with no crash report three times, each 2-2.5 min after a viewer joined = the player page's 120 s idle (AFK) disconnect aborting inside the Pixel Streaming media layer; ordinary tab closes did not crash it. Mitigations: viewer URL http://<host>/?AFKDetection=false&HoveringMouse=true, Tools/stream_keepalive.bat relaunches the sim after any exit — files: Tools/stream_keepalive.bat, README.md
- 2026-09-06 Hot-reloadable policy server file — PASS (live stream: line appended 15:23:17, server connected + hello + first reply within 1 s, 26 external decisions in the next 10 s; line removed 15:23:32, "-127.0.0.1:9100" and organisms unbound within 3 s, viewers stayed connected) — FSWRunSettings.PolicyServerFile (default Saved/policy_servers.txt) polled every PolicyFilePollSec (3 s) on wall clock in Tick before the substep loop; lines host:port=Lumen|Tecton|Both, '#' comments; effective set = -SWPolicy entries + file; added servers connect through the normal reconnect path, removed servers unbind their organisms; existing bindings are kept when a second server for the same species appears (only unbound organisms are assigned), so change a line's species to move organisms; no-policy path untouched (pure file stat). Tools/policy_probe.py scans a subnet for policy servers with a real hello/decide probe (--write appends to the file without duplicates), Tools/policy_servers.example.txt, run_sim --policy-file — files: SWTypes.h, SWPolicyClient.*, SWWorldManager.*, Tools/run_sim.py, Tools/policy_probe.py, Tools/policy_servers.example.txt, docs/POLICY_API.md, README.md, docs/CONTRIBUTING.md
- 2026-09-06 Scientist hooks — PASS (tester: regression 0 differing lines x2 vs the 2026-09-05 seed-7 reference; control file headless: 9 commands executed + 1 after a reset to seed 9, drought on/off, live set, speed, pause, malformed lines rejected; service: 4-run C/N job with Welch comparison, CSV md5 identical, 31/33 hostile probes rejected 4xx; 4 findings fixed before push: policy_file confined to Saved/, set requires a scope, notes limit validated, same-line-count rewrite detection in the control cursor) — Tools/experiment_service.py (HTTP 8800), Tools/scientist_client.py, docs/SCIENTIST_API.md; Saved/control.txt polled every 2 s (drought/speed/pause/set/reset/mode/note, commands.csv audit, HUD "ctrl"), Tools/control.py, docs/CONTROL_FILE.md; verified on the live stream (note + drought on/off at t=36/58) — files: SWTypes.h, SWWorldManager.*, SWLogger.*, SWHUD.cpp, Tools/*, docs/*
- 2026-09-06 Predator merged (Kalyani Guggilla, branch predator) — PASS (isolated worktree C:/Users/<you>/SW_predator: 3-way merge on base 6a70ec0 conflict-free, build clean, seed-7 default run byte-identical, bLeviathan=1 deterministic with 4 predation deaths / 90 s; merged into main with one HUD conflict resolved (ctrl + predation) and the DESIGN.md §4 entry) — her files were committed at the repo root from a 13:47 base; relocated to Source/SymbioticWorld. Renders sent to the user from the worktree. Off by default; live enable = control "set Settings.bLeviathan=1" + "reset" — files: SWLeviathan.*, SWProcMesh.*, SWTypes.h, SWWorldManager.*, SWHUD.cpp, DESIGN.md, README.md
- 2026-09-06 Symbiotic Lab merged (WKrohg, fork branch scientist-avatars 7c8b03a) + scientist avatar layer — PASS (Tools/build.bat clean, 19 steps, UBA 11 s; seed 7 C 120 s headless byte-identical to the 17:33 reference: population 46, agents 8264, births 63, deaths 41 rows, run_id ignored; render with `Lab.lab observe --embody` on 127.0.0.1:9000 + `--set "Settings.bLeviathan=1;Look.bScientistAvatars=1"`: all 8 field-team avatars joined 2.8 s after start, screenshots t0004/t0040; the capsule fallback + 34 uu labels are near-invisible at the demo camera distance, mannequin content absent) — 77-agent read-only review before the build (24 findings, fixes in aa74d25: quiet soft-loads, V re-apply, retire/cap 16, logical-clock staleness, reset drops the old run's trailing report; Lab: observer re-reports after reset, runner prefers LAB_SIM_SERVICE, evidence-id retry) — files: Source/SymbioticWorld/SWScientistAvatar.*, SWPolicyClient.*, SWWorldManager.*, SWPlayerController.*, SWTypes.h, Lab/, docs/POLICY_API.md §5, docs/CONTROL_FILE.md, DESIGN.md §4, README.md
- 2026-09-06 Stream supervisor relaunch storm — FIXED (Tools/stream_keepalive.bat launched from Git Bash resolved `timeout`/`find` to the GNU tools, so its wait loop failed instantly and spawned 47 UnrealEditor processes in 20 s; now uses %SystemRoot%\System32\tasklist.exe / findstr.exe like Tools/build.bat, and ping-based sleeps because Windows timeout.exe refuses to run without a console stdin, which caused a second, smaller storm) — files: Tools/stream_keepalive.bat
- 2026-09-11 Authored creature bodies (user's Blender Lumen/Tecton package: skeletal meshes, 3 LODs, idle/walk clips, authored materials) wired in, procedural bodies kept as the fallback — PASS (Tools/build.bat clean after a 76-agent adversarial review; headless seed 7 C 120 s with Look.bAuthoredCreatures=1 and =0 both byte-identical to the pre-change reference 20260911-055640 (agents 8264, births 63, deaths 41, population 46 rows; run_id stripped), seed 1 C 600 s identical to 20260911-061322 (agents 77996, births 521, deaths 388, population 234); windowed 1600x900 offscreen: authored bodies render, HUD marker above the body, -SWFollowSpecies=Lumen/Tecton follows the selected organism (screenshots SW_20260911-074648_*_t0004/t0040, SW_20260911-074751_*_t0006/20/30, SW_20260911-074849_*_t0006/20/30; fallback Look.bAuthoredCreatures=0 SW_20260911-074938_*_t0004); perf 50x seed 1 stat unit: 17.6 ms at 136 organisms, 20.2 ms at 179 (procedural 15.8 / 16.2; all organisms stuck at LOD 0 before the UpdateLODStatus fix gave 22.8 / 25.8), pose update 1.3 / 2.9 ms, LOD histogram 2/26/108 and 1/59/119, worst foot reach error 22-55 cm on steep ground, no LogSymbioticWorld warnings) — files: Source/SymbioticWorld/SWAgent.*, SWCreatureMeshComponent.* (new), SWWorldManager.cpp, SWCameraPawn.*, SWHUD.cpp, SWTypes.h, Tools/import_symbiotic_creatures.py (new), docs/CREATURE_RENDERING.md (new), README.md, DESIGN.md §4, CHECKLIST.md row 3, docs/CONTRIBUTING.md, docs/CONTROL_FILE.md, .gitignore; Content/Characters/Symbiotic stays local and untracked until the owner decides to commit it (46 MB)
- 2026-09-11 Bigger world for the authored creatures (rectangular 8000 x 5500 arena, 440 m valley, 36 m x 4 m river with two tributaries, 2x predator, spaced patches, Tecton rebalance, LEVIATHAN card with kills per species, -SWFollowSpecies=Leviathan) — PASS (Tools/build.bat clean after a 69-agent review (28 confirmed findings fixed, incl. a stale tributary-depth cache, the trace gradient skewed by rectangular cells, the soil-regrowth coupling severed by the 600 uu Tecton grazing radius, F key unable to leave the predator chase, minimap panel off-screen, inverted tree band); seed 7 twice 300 s headless byte-identical (24246 agent rows, 206 births, 152 deaths); balance seeds 1-3 mode C: 600 s Lumen 93/119/110, Tecton 30/19/19 (min 9), 1800 s Lumen 116/113/122, Tecton 104/36/47 (min 9), explore share 6.4-6.9 % / 6.7-8.4 %; renders SW_20260911-115230_*_t0004/t0090 (wide), SW_20260911-115308_*_t0090 (top-down, both tributaries), SW_20260911-115345_*_t0030 (predator breach), SW_20260911-115431/115508_*_t0020 (Lumen / Tecton follow); perf 50x seed 1 stat unit 15.8 ms at 95 organisms, 16.7 ms at 121 with the hack-build dressing counts (the raised counts tried first cost 19.5 / 22.1 ms, so they are not the default; CHECKLIST row 4 stays open); levers documented in DESIGN.md §4/§6b: Settings.WorldHalfSize/Y, PatchMinSpacing, ResourcePatchesA/B, Look.River*, RiverBranches, LeviathanScale, Tecton.*) — files: Source/SymbioticWorld/SWTypes.h, SWProcMesh.*, SWTraceField.*, SWWorldManager.*, SWEnvironment.cpp, SWHUD.*, SWLeviathan.cpp, SWCameraPawn.*, SWAgent.cpp, Lab/embodiment.py, Lab/live_observer.py, Tools/policy_server.py, Tools/policy_client_check.py, Tools/policy_probe.py, DESIGN.md, README.md, docs/CONTROL_FILE.md, docs/POLICY_API.md, docs/CREATURE_RENDERING.md
- 2026-09-14 Predator chase jitter fixed (user-reported: the hunting Leviathan read as jittery) — PASS (Tools/build.bat clean; seed 7 C 300 s predator-OFF byte-identical to a purpose-built pre-change baseline build on all four CSVs, run_id ignored, so the default path is untouched; predator-ON two runs byte-identical, and a rendered run (--windowed --offscreen, UpdateVisual every frame) byte-identical to the headless run, so the per-frame interpolation cannot reach the simulation; predation still works: seeds 1-3, 1800 s, Lumen taken 6/7/3 and Tecton 5/5/5 against 3/5/3 and 5/2/3 recorded in DESIGN.md 6b on the old chase, Lumen minimum 38-40, Tecton minimum 7-9 -- not a controlled comparison, since the predator's own RNG draws re-phase the shared stream and every change re-rolls the trajectory) — root cause: bang-bang steering. The chase flipped Direction whenever the prey was more than 60 uu away along X while one substep moves LeviathanChaseSpeed x LogicalSubstep = 150 uu, so it could not settle inside its own deadband, and the yaw is derived from Direction, so the 18 m body snapped end-for-end at the substep rate (~47 reversals per 10 s of chase in an offline replication of that rule). Fix: brake into the prey (step clamped to the distance left); never step against the facing (inside LeviathanChaseBand, 250 uu, it holds station) and commit to a new direction only outside that band; heading eased at LeviathanTurnRate (45 deg/s) with travel scaled by the remaining turn; prey kept until it leaves the water or passes LeviathanPreyHold x the sense radius; no hunting during the strike cooldown or a drought pause; lateral reach 1.2 x RiverWidth so no visible water is unstrikeable; belly clamp follows the bed at LeviathanBedFollowRate; body interpolated between substeps (ASWLeviathan::UpdateVisual, an absolute-transform child of a bare root) like the authored organisms. A first version (brake without the no-backwards rule) went through a 146-agent adversarial review, 8 lenses x 3 refute-by-default skeptics: 13 of 54 claims upheld, collapsing to three defects -- the heading froze on acquisition while the animal tracked the prey's X backwards (found by 5 of 8 lenses), the interpolated body was drawn toward the world origin before the first substep (Init recorded the spawn transform and spent the priming flag), and this entry's first evidence was overstated (a one-seed 12-vs-11 kill comparison, and a 4.5 deg yaw bound that is the clamp's own definition); all three are fixed here. Refuted: bed low-pass sinking the body, breach rise lifting it clear, prey-hold latching, sideways crabbing, CONTROL_FILE.md listing — files: SWLeviathan.*, SWTypes.h, SWWorldManager.cpp, DESIGN.md
- 2026-09-14 Scientist avatars animate, read from the demo camera, and can be followed; spec amended for human researcher avatars + the Leviathan predator — PASS (root cause: SWScientistAvatar loaded ABP_Manny/ABP_Quinn from /Game/Characters/Mannequins/Animations, which Epic's template pack never had, with LOAD_Quiet hiding the miss, and the capsule fallback /Engine/BasicShapes/Capsule does not exist either; now the MM_Idle / MF_Unarmed_Walk_Fwd / MF_Unarmed_Jog_Fwd clips (Content/Characters/Mannequins/Anims/Unarmed, Tools/import_mannequins.py) play as single-node animations picked from the avatar's smoothed on-screen speed, misses log a Warning, true 1.8 m scale kept (Lumen ~3.4 m, Tecton ~7 m), body paint tinted per scientist via the mannequin's "Paint Tint" parameter, the 34 uu TextRender label replaced by a screen-space HUD name tag + chevron, key G / -SWFollowScientist=<Name>|any chase-cam through the team; docs/SPEC_TEXT.txt lines 77/87 amended 14 Sep 2026 (human researcher avatars and the Leviathan allowed, neither a species). Verified: Tools/build.bat clean x4; seed 1 C 45 s --windowed --offscreen with Lab.lab observe --embody on 127.0.0.1:9000 and Look.bScientistAvatars=1: all 8 avatars "joined as mannequin", clips 7.57 / 1.50 / 1.77 s, 2 material slots tinted, no warnings; screenshots 0.5 s apart show the pose changing (upright -> legs apart mid-stride); from the start camera the 1.8 m bodies are ~14 px tall but all 8 tags are readable in both t=10 s and t=40 s frames (the earlier capsule + 34 uu labels were invisible); -SWFollowScientist=Vesper acquired Vesper 0.08 s after the team joined and the t=10 / t=40 frames are a close chase-cam of the walk clip with Ada (violet) and Vega (mint) tinted in view. Visual/camera only: no seeded draw, no sim state written; avatars exist only with a policy bridge attached, so no A/B determinism run applies. Known: two tags overlap when scientists stand within a couple of metres; the walk/jog stride speeds (170 / 380 uu/s) are constants because the clips are root-locked, so feet may slide slightly. Note: bare `python` on this machine resolves to a hermes-agent venv without numpy; the Lab observer needs the WindowsApps python.exe) — files: Source/SymbioticWorld/SWScientistAvatar.*, SWHUD.*, SWCameraPawn.*, SWPlayerController.*, SWWorldManager.h, Config/DefaultInput.ini, Tools/import_mannequins.py, docs/SPEC_TEXT.txt, docs/POLICY_API.md, DESIGN.md, README.md, Lab/README.md
- 2026-09-14 Avatar pass review follow-up — PASS (47-agent adversarial review of the avatar / tag / follow-key change: 14 findings, 6 confirmed, 8 refuted. Fixed: (1) the gait read the avatar's own on-screen motion, which is a 400 uu/s burst then a stop after every 0.5 s Lab report, so walk <-> jog flapped and restarted the clip about twice a second at 1x; the gait and the walking speed now follow the pace the reports imply (distance / sim-time gap, x time scale, gain 1.05, catch-up only when more than a report's travel behind), and each avatar logs its clip-change count at EndPlay: seed 1 C 45 s at 1x, all 8 avatars 2 gait changes each (initial idle + idle -> walk) against the ~80 predicted for the old estimator; (2) the help panel had grown by a line and, with an organism selected, overlapped the Tecton panel's bottom 6 px at 1600x900 — the G key is folded into the camera line, panel back to 130 px / SY-150, pixel scan of the --auto-select t=40 frame: Tecton border row 740, help accent rows 750-751; (3) -SWFollowScientist now warns once (after 5 sim-s) when either prerequisite is missing, not only the avatar flag: 12 s run with the flag on and no --policy logs "no policy bridge ...; nothing to follow"; (4) the scientist request is evaluated before the -SWFollowSpecies=Leviathan block, whose early return used to swallow it; (5) Tools/import_mannequins.py docstring no longer claims all of Content/Characters is ignored (only Mannequins/ is; Symbiotic/ is untracked on purpose). Refuted and left alone: a missing MM_Idle leaving a stopped avatar walking in place (already warned, degraded-content case), tag colour by join slot after a retire/rejoin) — files: Source/SymbioticWorld/SWScientistAvatar.*, SWHUD.cpp, SWCameraPawn.cpp, Tools/import_mannequins.py
- 2026-09-14 Build-partition determinism (user-reported: the same source at e256eda gave a different seed-7 run depending on which files UBT's adaptive unity build had excluded from the unity blob; runs 20260914-111527 vs 20260914-112035, 23,499 differing agents.csv lines) — FIXED, PASS — root cause is not floating point: SpawnPatches computed `RiverDistance(..., ChannelWidth, ...) / FMath::Max(ChannelWidth, 1.f)` in one expression; the order of the call (which writes ChannelWidth) and of the divisor's read is unspecified, MSVC read the written width (600 / 1000 uu) in the unity TU and the initial 1.f in the standalone TU, so the channel test `Ratio < 1.9` was effectively off whenever SWWorldManager.cpp was in the git working set (patches could land in the river; only the wetland test remained). Probe: temporary 9-significant-digit logging of every placement candidate (x, y, ratio, width, height, dry), score and the first two decisions per organism (1458 lines) — the first divergent quantity is candidate 0's ratio, 0.415400594 vs 249.240356 (exactly x600), every other value identical, so /fp:fast contributed nothing measurable; FPSemantics.Precise (needs a private PCH or NoPCHs) and bUseUnity=false were checked against UE 5.7 ModuleRules but are not needed. Fix: two statements (SWWorldManager.cpp). Verification: seed 7 C 300 s identical (0 differing lines, run_id stripped) across three partitions of the fixed source — every file in the blob (`Tools/build.bat -DisableAdaptiveUnity`), SWWorldManager.cpp excluded, and {SWAgent, SWEnvironment, SWLearner, SWProcMesh, SWTraceField, SWWorldManager} excluded — plus seed 7 120 s and seed 1 600 s identical between the first two; 200x and 50x give identical CSVs; the unity-build reference 20260914-111527 is reproduced exactly (the excluded-file run 20260914-112035 was the defective path), so clean-checkout references stand and only numbers measured in a build with SWWorldManager.cpp modified change. New references (lines, header included): seed 7 C 300 s agents 23715 / births 195 / deaths 160 / population 117; seed 7 C 120 s 7735 / 65 / 43 / 47; seed 1 C 600 s 54735 / 427 / 387 / 235. Perf 50x headless seed 1 600 s: 20.2 s wall before, 19.1 s after (noise; the fix adds no work). Open: DESIGN.md §6b balance numbers from 2026-09-11 may have been measured on the defective placement path (SWWorldManager.cpp was in the working set that session); re-measure before a balance decision leans on them — files: SWWorldManager.cpp, docs/CONTRIBUTING.md, DESIGN.md
- 2026-09-15 Field team targets organisms, keeps to land, crosses by jet ski (user: the scientists walked around aimlessly) — PASS (Lab/embodiment.py: each scientist walks to the nearest organism that fits their rule — Vesper nearest Lumen, Bastion nearest Tecton, Mendel nearest of the five youngest, the rest the nearest organism no colleague watches — keeps it unless another is 0.6x as far, stops 250 uu short and watches until it drifts 400 uu; routes on the hello's new `water_mask` (200 uu cells from the on_land test, 80x55, hello 5.2 KB) with A*, water 3x land, and rides a jet ski at 600 uu/s only when the target is across; the sim draws the jet ski (engine BasicShapes, hull in the scientist's colour) from its own terrain test against the drawn, drought-lowered surface. Two adversarial reviews before the commit: 16 findings then 4, fixed — targets had been picked from each decide message, which carries only the organisms due that substep (~1/10), so they flipped constantly: the field now remembers every organism's latest report and forgets it after 2.5 decision intervals; Mendel's youngest ranking ages those reports forward; the steering pole's pitch sign; the ski hovered 55 uu above drought water; a slow-motion jog after landing; the leash carried over to a new target; A* squeezed diagonally between water cells; colleagues' organisms could be taken; route searches are capped at 2 refreshes per message so the bridge stays inside the sim's 200 ms budget; docs/SPEC_TEXT.txt amended for the jet ski. Verified: Lab/tests/test_embodiment.py 16 pass; Tools/build.bat clean (the last header edit is comment-only); seed 1 C renders with Lab.lab observe --embody: 755/755, 935/935 and 2060/2060 bridge replies, 0 timeouts, 0.6-1.8 ms; target switches 0-14 per scientist per minute (max 19, Mendel 6-14 after his fix, was 12-24) against ~450 decide messages per minute; 240 sim-s drought run following Ada: she rides the jet ski for ~100 sim-s watching Tectons in the river, the pole meets the handlebar, the hull sits in the lowered water, she walks after landing. Known: during a drought the Lab (undropped mask) may report `jetski` while the avatar walks the exposed bed (documented in DESIGN.md / POLICY_API.md, mode is informational); the mask is fixed until a reset after a live `set Look.*`. Machine: C: had 1.0-1.7 GB free, the shader cache could not write (`Insufficient Storage`), so some frames rendered untextured and a render took 14 min; a hung river-levers editor (stuck at D3D12 init since 10:04) blocked Tools/build.bat until the user ended it) — files: Lab/embodiment.py, Lab/live_observer.py, Lab/tests/test_embodiment.py, Source/SymbioticWorld/SWScientistAvatar.*, SWWorldManager.cpp, docs/POLICY_API.md, docs/SPEC_TEXT.txt, DESIGN.md, Lab/README.md
- 2026-09-15 lumen-tecton branch verified for push (75e4e90..986c97e) + build-partition fix committed (27941a8) — PASS (session worktree fast-forwarded to 986c97e, clean full build 221 s; seed 7 C 300 s, seed 7 C 120 s pair and seed 1 C 600 s byte-identical to the goofy-aryabhata references 20260914-114829 / 115741 / 115748 (run_id stripped), so the five commits leave the no-policy path unchanged; the older lumen-tecton references (seed 7 300 s, 22422 agent rows) came from a working-set build of the unfixed source; predator-on seed 7 C 120 s pair byte-identical (Lumen 5, Tecton 7 taken); seeds 1-3 C 600 s no extinction (Lumen end 85/123/120, min 40; Tecton end 13/30/51, min 6); Lab tests 16/16; renders seed 1 C 45 s --windowed --offscreen: authored SK_Lumen / SK_Tecton (3 LODs) and the Leviathan (SW_20260915-124145_*_t0004/t0040; t0004 still shows shader warm-up in a fresh worktree), field team via Lab.lab observe --embody on 127.0.0.1:9000 with Look.bScientistAvatars=1 and -SWFollowScientist=any: 8/8 joined as mannequins, 0 bridge timeouts, Vesper crossing by jet ski (SW_20260915-125233_*_t0012/t0040). With the SpawnPatches fix: 4/4 reference runs byte-identical with SWWorldManager.cpp excluded from the unity blob, and again after Tools/build.bat -DisableAdaptiveUnity (a plain rebuild after the commit compiled nothing: UBT keeps a formerly modified file outside the blob until the blob is rebuilt). Cosmetic, not fixed: with a policy server connected the title card's "ext N" suffix runs into the GENERATION card subtitle) — files: Source/SymbioticWorld/SWWorldManager.cpp, DESIGN.md, docs/CONTRIBUTING.md, PROGRESS.md
- 2026-09-17 WKrohg's fork branch discourse-trajectory merged (8 commits: OpenRouter backend with per-scientist `model:`, Humboldt the PI with an agenda round and veto arbitration, Fisher revise-and-resubmit, `python -m Lab.lab loop`, dashboard collect/pause/reset/save/load, LAB_TURN_PACE) — PASS (only .gitignore conflicted, resolved keeping both `Content/Characters/Mannequins/` and `Lab/saves/`; pytest 25 passed, `python Lab/tests/test_smoke.py` and `test_openrouter.py` pass) — the smoke test needed a fix first: it asserts experiments park as awaiting-sim, true only without UE 5.7, so on this sim host it executed five experiments for real and wrote their runs into the repo's Saved/ whatever LAB_SAVED said; it now points LAB_SIM_SERVICE at a dead port — files: Lab/*, .gitignore, Lab/tests/test_smoke.py
- 2026-09-17 Recorded demo takes + Lab duty cycle + policy back-off — PASS (control file gains `at=<sim_time> <command>` — 17 beats fired within 0.1 s of their scheduled sim time, pending list cleared on reset — plus `cam=x,y,z,pitch,yaw`, `follow=<target>`, `caption=<text>` (Look.CaptionSeconds 6, drawn even when Look.bShowHUD=0), Look.bShowLabPanel off by default, `run_sim.py --res WxH` and `--no-console`, `Tools/demo_captions.py` turning commands.csv into an .srt; predation reads on camera now — plume sized to the animal, stand-in body falling 1.5x its own measured height and clamped to the riverbed, kill feed under the LEVIATHAN card, minimap mark, all gated by Look.bPredationEffects; Lab/duty.py splits fieldwork (LAB_FIELD_WINDOWS windows) from a meeting at a camp on land (-4400,3410) where Humboldt the PI always stands, witnessed evidence minted only in the field and instrument windows in both, `loop` waiting on the phase; Lab concurrency fixed after a take froze the world — WAL, each meeting turn committed as spoken, observer writes skippable on a 250 ms busy wait, evidence flush retried across a meeting's gaps: worst observer block 5413 ms -> 349 ms and 36/47 turns visible mid-meeting instead of none; policy back-off (Settings.PolicyTimeoutBackoffAfter 3, PolicyBackoffStartSec 1.0, PolicyBackoffMaxSec 10.0) took a wedged server from 1263 s of wall clock per 5 sim-seconds to 9.4 s per 120 sim-seconds, CSVs identical to an unreachable port; determinism unchanged — seed 7 C 120 s 7735/65/43/47 and predator-on seed 4 7543/75/49/47, both 0 differing lines; tests pytest 25 passed plus test_embodiment, test_smoke and test_openrouter; takes captured with ffmpeg gdigrab + h264_nvenc at 1998x1498, final cut 3m59s on seed 4 — 1x for the predator and inspector beats, 50x to sim 2900 reaching generation 48 with Lumen inherited alpha 0.13 -> 0.25, then 1x for the drought) — known cosmetic gaps: the title card's "ext N" suffix overlaps the GENERATION card, and scientist name tags pile up at camp — files: Source/SymbioticWorld/SWKillMark.*, SWPolicyClient.*, SWWorldManager.*, SWHUD.*, SWTypes.h, SWScientistAvatar.cpp, Lab/duty.py, Lab/db.py, Lab/live_observer.py, Lab/embodiment.py, Lab/lab.py, Lab/ui_server.py, Lab/ui/index.html, Tools/run_sim.py, Tools/control.py, Tools/demo_captions.py, docs/CONTROL_FILE.md, docs/POLICY_API.md
- 2026-09-17 Experiment designer — PASS — audit of the 10 executed experiments found 3 distinct designs and 6 re-runs of one failed drought contrast (X-005..X-010, identical -0.009267 each time). Four causes, all fixed: (1) Lab/llm.py `_protocol` was a 3-branch keyword router and every challenge claim contains "drought", so mode A and the predator / MutationSigma / WeightNovelty knobs were unreachable -> replaced with a 12-row design bench (arms, preregistered metric, direction, threshold per row; 20 claims now map to 12 distinct designs, was 3); (2) victory.sync_questions rewrites a question's text every meeting, so the same unmet condition minted a fresh card each time (H-015..H-020) -> card identity is now the condition text with the [status] tag and parenthetical tail stripped (balanced-bracket scan: the tail contains "experiment(s)"), and those six cards were retired as superseded; (3) the design slot took the NEWEST card while the stance round only sees the 4 newest (MAX_ACTIVE_CARDS_PER_MEETING), so the seeded H-005..H-007 starved for 17 meetings -> least-tested-card-first, max 2 experiments per card; (4) no protocol-level dedupe -> an identical arms+metric+seeds+duration draft is filed as `duplicate` naming the original (10 filed this session, 0 sim time). Plus 4 seeded conjectures for mechanisms that landed after the seed list (predation floor, predation vs epsilon, novelty bonus, inheritance fidelity) and mode A + the real knob names in the DESIGN prompt. 20 meetings queued 10 distinct protocols; 64 runs executed in ~15 min: X-017 C vs mode A q_drift 1.6288 vs 0.0000 (pass) closes OQ-VC-3 so ALL SIX victory conditions now read met; X-013 wI=0.10 vs 0 lumen_end_n 121.7 vs 112.3 (pass, H-006 supported); X-014 env_effect 0.5502 vs 0.4767 under drought (pass, H-007 supported); X-012 epsilon 0.2211 vs 0.2673 and X-019 0.2369 vs 0.2673 (both opposite -> H-005, H-022 refuted: drought AND predation select for LOWER exploration); X-015 regen-4 vs 6 at 1800 s x 5 seeds min_n 35.0 vs 35.8 (no_difference — the stable band holds down to regen 4); X-018 predation floor 38 vs 40, X-011 MutationSigma 0.06 corr 0.8919 vs 0.9217, X-030 greedy_changed 58.178 vs 57.666 (all no_difference; H-014 honestly demoted supported -> conjecture). 70 preregistered predictions scored, 26 right (37%), mean Brier 0.719 (Ada 0.567 best, Mendel 0.885 worst). KNOWN FLAW: X-020's bench row preregistered lumen_min_n for the novelty claim and both arms returned exactly 40.0 — min_n is the early-run trough, reached before wN can matter; that row needs a late-run metric and a re-run. Files: Lab/llm.py, Lab/meeting.py, Lab/seed_content.py, Lab/ui/index.html (duplicate/superseded pills), CHECKLIST.md
- 2026-09-17 Lab clip take 3 — PASS — SymbioticWorld_lab_clip_20260917-164825.mp4 (2400x1600, 284 s, 37.7 MB): World with the sim streaming inside the dashboard and the duty card reading "in the field, 2 of 2 windows left", Conversation with meeting 40 forming live (39 -> 40 in the header), Experiments showing X-030's protocol, measured arms and 7 Brier-scored predictions plus X-029's DUPLICATE pill, Registry, Data. Two takes were lost first: a shared Chrome profile restored the previous take's tabs (the World segment drifted to Conversation at ~50 s), and then the desktop capture recorded the Claude app because a kiosk window that is pinned but not raised still sits behind it — Windows denies SetForegroundWindow to a non-foreground process, so pin_chrome.ps1 now drops SPI_SETFOREGROUNDLOCKTIMEOUT, attaches the foreground thread's input queue, taps Alt, raises and pins HWND_TOPMOST, and the script refuses to record unless it reads back foreground=chrome. Every take now ends by stacking each segment's nav bar at 10/50/90% into one strip image for verification. Files: Tools/ unchanged; scratchpad lab_clip4.sh, pin_chrome.ps1
- 2026-09-17 X-020 re-run + Vega annex unwedged — PASS — X-020's null was uninformative: it preregistered lumen_min_n for the novelty-bonus claim and both arms returned exactly 40.0, because min_n is the opening-seconds trough. Bench row now reads lumen_end_n, the same metric and 3.0 threshold X-013 moved by +9.33, and the lesson is caveat H-025 (min_n/tecton_min_n cannot test a late-run claim), which every future DESIGN prompt carries. X-033 re-ran it: treatment 122.3333 vs control 121.6667, diff +0.6667 -> no_difference, fail — now an informative null, H-023 stays a conjecture rather than being refuted; 3 of 7 agents called it (Brier 0.45). The design slot also learned to retry past a duplicate (MAX_DESIGN_ATTEMPTS=4) so a meeting that catches a repeat still designs something real: meeting 41 filed X-031 and X-032 as duplicates, then queued X-033. SEPARATE BUG found while waiting: `Lab/datasci.py` annex re-fitted EVERY run ever ingested on every session and re-loaded all of them twice more for the two figures, then discarded the result (the insert dedupes only after load_run parses the CSVs); one session burned 1859 s of CPU there and had to be killed, and the earlier sessions that never finished left ~140 runs unforecast. Fixed: skip a run whose forecasts exist, skip `live:` bridge rows that have no directory at all (22 of them were re-read forever), one disk read per run shared by both figures and both species, PLOT_MAX_RUNS=60 (a 264-line chart with a 264-entry legend is unreadable and was most of the cost), FORECAST_BUDGET=40 newest-first so a backlog can never hold a session hostage. Measured: annex 19.9 s steady state, `session --meetings 1` end to end 85 s (was: never completed), 956 forecasts stored and 684 verified against a longer run. Files: Lab/llm.py, Lab/meeting.py, Lab/seed_content.py, Lab/datasci.py
- 2026-09-18 Claude CLI backend + first session on Claude models — PASS — ClaudeCLILLM rewritten against the installed CLI (2.1.258), every flag checked in its --help rather than assumed: --json-schema carries the round's schema so the CLI enforces the response shape, --output-format json returns an envelope whose `result` is the JSON string, and the call is hermetic (--tools "", --strict-mcp-config, --no-session-persistence, host CLAUDE_CODE_* stripped from the child env) because this is the same CLI that runs the repo's coding agents. Prompt on stdin (an evidence prompt would overrun the Windows argv limit); per-scientist `claude_model:` with Fisher, Karla and Humboldt on claude-sonnet-5 as they already were for OpenRouter. Probe: 11 of 11 response kinds returned schema-valid JSON, $0.276, 4.5 min. The probe caught two bugs that would have poisoned a real run: the CLI emits UTF-8 while Windows text mode decoded cp1252 (every em dash became mojibake on its way into the DB), and Fisher proposed Settings.WeightTraceY, which does not exist — the sim only WARNS about an unknown -SWSet field and runs anyway, so that arm would have been identical to its control while the verdict was recorded as real; runner.unknown_set_fields() now parses the field names out of SWTypes.h per scope and the design round gives one corrective attempt naming the offending field. Session (meeting 44, 7 scientists, ~60 calls): Ada proposed H-026/H-027 from the evidence, two disputes docketed (one on lumen_predation_frac, a metric the keyword router could never reach), Fisher designed X-036 in mode B — and refused the docketed crux metric, citing caveat H-025 by name: "min_n is the opening-seconds trough (X-020: both arms hit exactly 40.0)". That is the caveat written from X-020's failure two hours earlier, read by a different scientist and changing a later design. X-036: regen-3 vs regen-6, lumen_end_n 43.0 vs 111.5 across 2 seeds. TWO BUGS FIXED FROM THAT RUN: (1) report.generate wrote its markdown without an encoding and died on a model's Δ (UnicodeEncodeError, cp1252) — every text read/write in Lab is now explicitly UTF-8, and the 291 KB transcript containing that character writes clean; (2) _validate_protocol inherited the docketed crux's direction even when Fisher had substituted the metric, so a result six of seven scientists correctly predicted (treatment_lower) was recorded as `opposite` and refuted H-027 — Karla flagged the mismatch in review but a direction error is not veto grounds, so it is now right in code: the crux's direction applies only to the crux's own metric. Also: `python -m Lab.lab textbook` on a fresh clone printed an empty object because seeding only happened inside a session (PRD §5 promises no cold start); it now seeds silently first and prints 12 cards + 9 open questions. A session on a hosted backend prints what it spent. Files: Lab/llm.py, Lab/meeting.py, Lab/runner.py, Lab/profiles.py, Lab/lab.py, Lab/report.py, Lab/ui_server.py, Lab/profiles/{fisher,humboldt,karla}.yaml, .gitignore
- 2026-09-18 Clean-clone verification + test suite — PASS — `git clone` of the published main into a temp dir: Tools/build.bat exit 0 in 72 s (UHT ran, 1.1 MB module DLL), then the README's own headless command (run_sim.py --mode C --seed 1 --duration 120 --analyze) exit 0 with Lumen n=77, Tecton n=14, median L1 drift 0.987, greedy action changed in 53% — and the Megascans LoadErrors are exactly the documented procedural-only fallback for a clone without the Electric Dreams migration. Every relative README link resolves; Tools/run_sim.py and Analysis/analyze_run.py answer --help; the stdlib policy server answered policy_client_check in 1.94/1.14/0.78 ms. Tests: 25 pytest passed, test_smoke PASSED (Vega: 24 forecasts, 4 figures, quarantine holds), test_openrouter passed. OpenRouter itself is unverifiable live: no OPENROUTER_API_KEY is set anywhere. Key audit: no secret-shaped string in any tracked file or anywhere in history (all refs, pickaxe), no .env/.pem in the tree, the key is read from the environment and used only in an Authorization header; .gitignore gained .env/*.env/*.pem/*.key/secrets.json and the coverage was verified with a dummy file.
- 2026-09-18 The epsilon lead, investigated and withdrawn — PASS (negative result) — X-012 and X-019 both reported that pressure selects for LOWER inherited exploration (drought 0.2211 vs 0.2673, predation 0.2369 vs 0.2673), which looked like the most interesting thing the lab had found. It is not real. Mechanism first, from logs already on disk: the selection differential on epsilon (mean parent_epsilon over all births minus the standing population mean) is -0.0009 in the drought arm and +0.0036 in the predation arm — reproduction is not favouring low-epsilon parents anywhere. Drought: the arm leaves 7, 32 and 18 survivors per seed with a seed-to-seed SD of 0.0606 against a claimed 0.046 effect, so a 'mean epsilon' over seven individuals was being read as selection. Predation: only 14 kills across 3 runs (vs ~2070 deaths), and the killed carried epsilon 0.032 BELOW the population mean, i.e. the Leviathan takes the cautious ones. Then the confound: SWLeviathan.cpp draws from Manager->GetRng(), the shared seeded stream, so predator-on and predator-off are not the same world plus a predator but two different draws from the same seed. Decisive re-run at 8 seeds per arm (16 runs, 900 s): ON 0.2525 (SD 0.0238) vs OFF 0.2470 (SD 0.0377), diff +0.0054 at 0.3 SE — the effect vanishes and flips sign; the 3-seed result was which three seeds got drawn (seeds 5 and 7 put the OFF arm at 0.1837 and 0.1949). Selection differentials in the 8-seed arms are identical (+0.0061 ON, +0.0062 OFF) and kills are ~6 per run. Consequences: caveat H-028 filed (end epsilon has a seed-to-seed SD near 0.03, so a 3-seed contrast cannot resolve 0.01-0.03; >=8 seeds for any epsilon or alpha claim), the unsupported refutations of H-005 and H-022 withdrawn to conjecture with the reason recorded on each card, and the design bench's two epsilon rows now preregister 8 seeds at a 0.03 band with _threshold_for raised to match. The lab's default 3 seeds x 0.01 threshold on a genome mean was below its own noise floor and would have manufactured effects indefinitely. Files: Lab/llm.py, Lab/lab.sqlite (registry), PROGRESS.md
- 2026-09-18 Selection on exploration: the instrument was the problem — PASS — chasing the withdrawn epsilon lead to its root. What epsilon IS: the bandit's policy randomness (SWLearner.cpp:58, take a uniformly random feasible action with probability epsilon), range 0.01-0.50, founders 0.20, mutation sigma 0.03 per birth — every card so far had argued it as foraging behaviour. Within-run fitness gradient over 16 runs and 6585 Lumen: below epsilon 0.15 an individual leaves 0.966 offspring against 1.105 above it, a 12.6% penalty, flat above ~0.19; it survives inside every alpha quartile (r +0.046 to +0.070) and `social` shows nothing (r -0.010) as a control. So selection on epsilon is real, POSITIVE and one-sided — the original conjecture direction was right and the refutation was doubly wrong. Mechanism, after three stories died on the data (including 'low epsilon accrues energy slower': low-epsilon agents hold MORE mean energy, 51.4 vs 46.5, and spend MORE life above the reproduction threshold, 0.74% vs 0.27%): every energy observable is downstream of what it would explain, because ReproCost=40 knocks a frequent reproducer down at each birth and MaxPopulation=220 (reached at 217) queues rich agents above the line. The unconfounded measure is reward per decision over 1.17M snapshots: 0.0158 / 0.0163 / 0.0183 / 0.0195 by epsilon band, rising on the GREEDY decisions too (0.0157 -> 0.0199, +27%), against an exploration tax of ~0.001 per explored decision. Epsilon buys policy quality; it is not a survival trait (lifespan flat 122.4 -> 118.5 s), while alpha is (starvation 29.8% vs 23.2%, death at 114.8 s vs 124.3 s). INSTRUMENT: means cannot see a one-sided trait, and mode N is not a clean null for a mean — it holds population at exactly 40 against mode C's 122, so it drifts harder (SD 0.074 vs 0.038) and earns more reward per decision (0.0294 vs 0.0174, 10.9 SE) purely on density (caveat H-029). Added 14 statistics: end_p10_{epsilon,alpha}, end_frac_eps_low, mean_reward, and the within-run selection gradients selection_r_{epsilon,alpha,social} per species, whose null is exact because neutral mode picks parents at random. The gradient answers the question the arm contrast could not: C +0.0749 vs N -0.0501 on epsilon (3.1 SE), C +0.0783 vs N -0.0159 on alpha (2.9 SE), social -0.0127 vs +0.0199 (1.2 SE, the control). Bench rows now preregister the gradient at 8 seeds. Files: Lab/evidence.py, Lab/runner.py, Lab/llm.py, Lab/lab.sqlite (caveat H-029)
- 2026-09-18 Clean editor startup + Play-in-Editor verified — PASS — PROJECT CLOSED. Opening the project on a machine without SM6 in the targeted shader formats prompted to enable D3D12 Shader Model 6 and logged warnings the project itself caused; all fixed at the source (89483e2): D3D12TargetedShaderFormats now includes PCD3D_SM6 (the project renders with DX12, virtual shadow maps and Nanite); M_SW_Scan carries bUsedWithNanite, committed as saved and set by Tools/make_materials.py so regeneration cannot lose it, verified in-engine (used_with_nanite=True); r.VolumetricFog=1 removed because scalability already enables fog at High/Epic/Cine and forcing it overrode a weak GPU's quality choice; r.Lumen.TranslucencyReflections.FrontLayer.Enable=0 removed as redundant with EnableForProject=0. Result: a headless start logs 0 warnings and 0 errors (was 2); a full editor start takes 11.9 s and logs 13 warning lines, every one from the engine install or the network (12 missing Slate/VisionOS/EaseCurveTool icons in UE 5.7.3 - fixed by the launcher's Verify, 1 EditorPerf menu insert), none from the project; the renderer reports D3D12 at SM6. The last CHECKLIST item, Play-in-Editor keys, was confirmed at the keyboard by the project owner; the editor log shows four PIE sessions with zero simulation warnings or errors. All 21 items are ticked. Open research threads for a future session, not loose ends: the lab now measures selection with the within-run gradient (lumen_selection_r_*) and should re-derive the drought and predation questions with it at 8 seeds; the river-crossing experiments are preserved as commits on their own branches (river-crossings 00e0c39, river-levers 245c2e7) and should be re-derived on current main rather than rebased.
- 2026-09-18 River crossings, predation and place inheritance — PASS (measurement) — ported the river-crossing counter from branch claude/river-crossings onto main and added position logging: agents.csv gains river_crossings,x,y,river_dist,in_water; deaths.csv gains river_crossings,mid_crossing; population.csv gains river_crossings,mean_river_dist,frac_in_water (river_dist = |Y - main-channel centreline|). Logging only: seed 7 mode C 120 s still gives exactly 7735/65/43/47 lines after each change. Mechanism first, from the code: nothing perceives the Leviathan (SWAgent never references it), a gamma=0 bandit cannot credit a consequence, being eaten produces no update, and newborns get a fresh random Q-table (only alpha/epsilon/social/e are inherited), so predation cannot be LEARNED as a deterrent. Crossings (16 runs x 1800 s each mode): mode C 0.4 vs 0.5 per run, predator off/on - 7 in eight simulated hours, nobody twice; mode N 20.9 -> 5.2 with the predator (5.0 SE), as a filter: 11.2 kills per run mid-crossing and repeat crossers 25 -> 1. The user's hypothesis (adventurous = more likely to cross, they die, the population steers away): crossing IS heritable - a child crosses 19.0% of the time if its parent did against 2.4% (7.8x, mode N, predator off) - but not one inherited gene predicts it (alpha -0.038, epsilon -0.013, social -0.019, e -0.010); crossers and victims carry LOWER epsilon than average. It is inherited through BIRTHPLACE: a newborn's distance from the river matches its parent's at r = 0.967 (5958 pairs, mean gap 2.5 m). With the predator on, children of crossers crossed 0 of 30 times - it prunes river-side families. The early-kill-then-clumping pattern the user saw in mode C is the founders: RandomArenaPoint scatters them (38% start within 20 m of the river vs 0.2% of those born later), 78% of kills land in the first minute, and the exodus is IDENTICAL without the predator (41% in water at 0-15 s -> 0.1% by 60-120 s, mean distance 29 -> 49 m, both arms). Scale, corrected here because an older note said km: the valley is 110 m across, the water spans ~36 m, food sits ~45-50 m from the centreline (~26 m beyond the bank), a Lumen walks 3.5 m/s - the river is ~8 s away; short explore legs (~7-17 m) just never carry them there. Files: Source/SymbioticWorld/SWAgent.*, SWLogger.*, SWWorldManager.*
